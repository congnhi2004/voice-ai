# Acceptance Checklist

Use this file as release evidence. Each item should be marked with status and concrete proof before launch.

Status values: `Not started`, `In progress`, `Pass`, `Fail`, `Blocked`.

| Area | Criterion | Verification | Status | Evidence |
| --- | --- | --- | --- | --- |
| Health | `GET /healthz` returns `200` with service status. | Curl local/staging endpoint. | Pass | 2026-05-10 QA: public `GET http://103.27.237.252:8080/healthz` returned 200 with status ok, service voice-ai, version 0.1.0. |
| Readiness | `GET /readyz` detects provider, storage, and MLflow readiness. | Run with valid and missing Google credentials. | Pass | 2026-05-10 final gate: public `/readyz` returned 200 with local provider/storage/video ready, FFmpeg available, and MLflow configured/ready. Caveat: successful public requests still fail MLflow API host validation and return `mlflow_run_id: null`. |
| Auth | Synthesis rejects missing/invalid API key. | Curl without key and with bad key. | Pass | 2026-05-10 QA: backend pytest covers missing, invalid, and valid API key behavior; public demo is intentionally unauthenticated because no API keys are configured. |
| CORS | Production allows only configured origins. | Browser/API test from allowed and disallowed origins. | Blocked | 2026-05-10 QA PARTIAL: public frontend can call backend successfully from `:4174`; disallowed-origin production CORS verification was not run. |
| Voices | `GET /v1/voices` returns documented schema. | Contract test. | Pass | 2026-05-10 final gate: public `/v1/voices?language_code=vi-VN` returned HTTP 200; backend contract tests passed. |
| Local fallback | CI can synthesize without Google credentials. | CI test log. | Pass | 2026-05-10 final gate: backend pytest local fallback synthesis passed; public synthesize returned 200, job `tts_41d0def71fd744fbac10447580d0e7d0`, 96,044-byte RIFF/WAV. CI itself was not run. |
| Google provider | Staging can synthesize through Google Cloud Text-to-Speech. | Staging curl plus playable audio. | Blocked | 2026-05-10 QA: no Google credentials or Google-backed staging URL provided; only local fallback provider tested. |
| Synthesis response | Response includes `job_id`, `audio_url`, `audio_path`, `duration_ms`, `latency_ms`, `provider`, and metadata. | Contract test and sample response. | Fail | 2026-05-10 final gate: public synthesize returned required TTS/audio fields and a valid `audio_url`, but final-gate requirement failed because `observability.mlflow_run_id` was `null` with MLflow 403 `Invalid Host header`. |
| Audio serving | Local `/audio/{filename}` returns playable generated audio. | Curl/browser playback. | Pass | 2026-05-10 final gate: API-returned audio URL downloaded to `docs/subagents/final-gate-tts-audio-20260510.wav`, 96,044 bytes, RIFF/WAVE PCM mono 24 kHz. |
| Production storage | Production audio is durable and not dependent on Cloud Run filesystem. | Bucket/object evidence or signed URL evidence. | Blocked | 2026-05-10 QA: public API returns local `/app/data/...` artifact paths; no bucket/object/signed URL evidence provided. |
| MLflow | Each successful synthesis creates an MLflow run. | MLflow run URL or run id. | Fail | 2026-05-10 final gate: public TTS and video localization responses had `mlflow_run_id: null`; warning says MLflow `/api/2.0/mlflow/experiments/get-by-name` returned 403 `Invalid Host header`. |
| Privacy | Logs do not include raw user text by default. | Log sample review. | Pass | 2026-05-10 QA: backend tests and public responses expose request/job metadata and counts/checksums; no secrets were exposed in QA artifacts. Direct deployed log sample was not available. |
| Errors | Validation, auth, provider, quota, and storage failures return structured JSON. | Error contract tests. | Fail | 2026-05-10 final gate: backend pytest validation/auth/size/provider readiness tests passed, but public fake MP4 upload returned HTTP 200 `status: succeeded` instead of a clear invalid-video error. |
| Limits | Max input characters enforced. | Boundary tests. | Pass | 2026-05-10 QA: backend pytest `test_max_input_chars_returns_contract_error` passed with 413 and `input_too_large`. |
| Deployment | Staging deploy pipeline completes from a clean commit. | CI/CD run URL. | Blocked | 2026-05-10 final gate: `docker compose config --quiet` passed and local tmux services are up, but no CI/CD run URL or clean-commit deploy evidence was provided. |
| Rollback | Cloud Run rollback path tested. | Revision rollback evidence. | Blocked | 2026-05-10 QA: Cloud Run deploy/rollback not executed; no revision rollback evidence provided. |
| Observability | Dashboard has request, latency, error, and provider panels. | Dashboard link/screenshot. | Fail | 2026-05-10 final gate: runtime MLflow tracking fails with 403 `Invalid Host header`; no dashboard link/screenshot for request, latency, error, and provider panels provided. |
| Alerts | Alerts exist for error rate, latency, provider auth, quota, and storage failures. | Alert policy links. | Blocked | 2026-05-10 QA: no alert policy links or deployed monitoring evidence provided. |
| Documentation | API contract and runbook match deployed behavior. | QA doc review signoff. | Fail | 2026-05-10 final gate: docs broadly match demo API shape, but deployed behavior fails MLflow run evidence and invalid-video rejection. See `docs/subagents/qa-report.md`. |
| Public frontend | Public frontend loads on desktop and mobile with a usable first viewport and no crypto runtime error. | Playwright screenshot/browser check. | Pass | 2026-05-10 final gate: screenshots `docs/subagents/final-gate-public-desktop-20260510.png` and `docs/subagents/final-gate-public-mobile-20260510.png`; browser check found no page/console/crypto errors. |
| Frontend API default | Public frontend defaults to the public backend, not localhost. | Browser value check. | Pass | 2026-05-10 final gate: `docs/subagents/final-gate-frontend-browser-check-20260510.json` shows `#base-url` is `http://103.27.237.252:8080` on desktop and mobile. |
| Video invalid upload | Invalid/fake MP4 fails clearly. | Public upload smoke. | Fail | 2026-05-10 final gate: `docs/subagents/final-gate-video-fake-response-20260510.txt` shows a 23-byte fake MP4 returned HTTP 200 `status: succeeded`. |
| Video valid upload | Valid MP4 localization returns downloadable SRT, WAV, and playable final MP4. | Generated fixture upload plus artifact download/probe. | Pass | 2026-05-10 final gate with prototype caveat: generated fixture `docs/subagents/final-gate-valid-source-20260510.mp4`; final `docs/subagents/final-gate-video-localized-20260510.mp4` has H.264 video, AAC audio, and mov_text subtitle streams per `docs/subagents/final-gate-video-ffprobe-20260510.json`. Provider remains local demo fallback and MLflow run id is null. |
| Automated tests | Backend pytest, frontend lint/unit/build, and desktop/mobile Playwright pass. | Local command output. | Pass | 2026-05-10 final gate: ran with `taskset -c 0-3`; backend pytest 11 passed, 3 skipped; frontend lint, unit, build passed; Playwright desktop 2 passed and mobile 2 passed with LD_LIBRARY_PATH browser dependency workaround. |
